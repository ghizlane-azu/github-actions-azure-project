- name: Zip content
  run: zip -r release.zip . -x "*.git*"

- name: Deploy to Azure
  uses: azure/webapps-deploy@v3
  with:
    app-name: 'visit-counter-app'
    publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
    package: release.zip
